---
'cyber-figma': minor
---

Ship the agent-plugin layer. The package root is the plugin root, so the tarball now carries the Agent Plugins 1.0.0 manifests (`plugin.json`, `mcp.json`), the per-vendor manifests for Claude Code, Cursor, and Codex, and three skills: `init-figma`, `inspect-figma-file`, and `export-figma-assets`.
